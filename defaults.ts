let defaults = {
    // sizeAreaOptions: Array.from(new Array(25),
    //     (item, index) => (
    //         { value: (index+1).toString(), label: (index+1).toString() }
    //     )
    // ),
    // titleSizeControl: 'Размерность',
    // titleGameControl: 'Управление игрой',
};
export default defaults;
